import { createChatBotMessage } from 'react-chatbot-kit';

const config = {
    initialMessages: [createChatBotMessage(`Hello. How can I help you with your itinerary today?`)],
};

export default config;